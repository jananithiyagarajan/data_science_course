# What is a JOIN in SQL?

A JOIN is used to combine rows from two or more tables based on a related column (usually a primary key and foreign key).


# Types of Joins

| JOIN Type  | Returns                   |
| ---------- | ------------------------- |
| INNER JOIN | Only matching rows        |
| LEFT JOIN  | All left + matching right |
| RIGHT JOIN | All right + matching left |
| FULL JOIN  | All rows from both tables |
| CROSS JOIN | All combinations          |
| SELF JOIN  | Table joined to itself    |

<p align="center">
    <img width="572" height="384" alt="image" src="https://github.com/user-attachments/assets/036c3c67-e2f7-4333-9155-fa99ffef9fdb" />

</p>
**Create Table: `departments3`**

```
CREATE TABLE departments3 (
    dept_id INT PRIMARY KEY,
    dept_name VARCHAR(50),
    location VARCHAR(50)
);
```
<img width="894" height="193" alt="Screenshot from 2026-03-17 12-31-42" src="https://github.com/user-attachments/assets/1c7faa28-052b-422e-a770-cb3e4775a5e2" />

```
INSERT INTO departments3 (dept_id, dept_name, location) VALUES
(101, 'HR', 'Chennai'),
(102, 'IT', 'Bengaluru'),
(103, 'Finance', 'Mumbai'),
(104, 'Marketing', 'Delhi'),
(105, 'Admin', 'Hyderabad'),
(106, 'Sales', 'Pune'),
(107, 'Support', 'Kochi'),
(108, 'R&D', 'Noida'),
(109, 'Legal', 'Kolkata'),
(110, 'Operations', 'Jaipur');
```
<img width="894" height="349" alt="Screenshot from 2026-03-17 12-32-21" src="https://github.com/user-attachments/assets/b7072e57-e055-4318-849e-6d75ca756a7c" />

**Create Table: `employees3`**

```
CREATE TABLE employees3 (
    emp_id INT PRIMARY KEY,
    emp_name VARCHAR(50),
    dept_id INT,
    salary INT,
    FOREIGN KEY (dept_id) REFERENCES departments3(dept_id)
);
```
<img width="894" height="236" alt="Screenshot from 2026-03-17 12-32-50" src="https://github.com/user-attachments/assets/50bc8649-1454-4061-95d0-3a7756064a16" />

```
INSERT INTO employees3 (emp_id, emp_name, dept_id, salary) VALUES
(1, 'Arjun', 101, 35000),
(2, 'Meera', 102, 55000),
(3, 'Rahul', 103, 45000),
(4, 'Sneha', 102, 60000),
(5, 'Kiran', 104, 30000),
(6, 'Priya', 103, 48000),
(7, 'Varun', 102, 62000),
(8, 'Divya', 101, 37000),
(9, 'Sanjay', NULL, 32000),
(10, 'Anitha', 105, 40000);
```
<img width="894" height="348" alt="Screenshot from 2026-03-17 12-33-17" src="https://github.com/user-attachments/assets/9485228f-d5c0-42a4-91b0-e4f2033a6a52" />

## **1. INNER JOIN**

An INNER JOIN is used to retrieve only the records that have matching values in both tables based on a specified condition.

<p align="center">
    <img width="1007" height="504" alt="image" src="https://github.com/user-attachments/assets/458503b5-99a2-4f17-8714-568a7bb6d1ab" />

</p>

## Example Questions

**Q1. List employee name and department name for employees who belong to a department.**

```
SELECT e.emp_name, d.dept_name
FROM employees3 e
INNER JOIN departments3 d
ON e.dept_id = d.dept_id;
```

**Q2. Find employees working in the IT department.**

```
SELECT e.emp_name, d.dept_name
FROM employees3 e
INNER JOIN departments3 d
ON e.dept_id = d.dept_id
WHERE d.dept_name = 'IT';
```

**Q3. Show employee name, salary, and department location.**

```
SELECT e.emp_name, e.salary, d.location
FROM employees3 e
INNER JOIN departments3 d
ON e.dept_id = d.dept_id;
```

**Q4. List employees whose department is located in Chennai.**

```
SELECT e.emp_name, d.location
FROM employees3 e
INNER JOIN departments3 d
ON e.dept_id = d.dept_id
WHERE d.location = 'Chennai';
```

**Q5. Count number of employees in each department.**

```
SELECT d.dept_name, COUNT(e.emp_id) AS total_employees
FROM employees3 e
INNER JOIN departments3 d
ON e.dept_id = d.dept_id
GROUP BY d.dept_name;
```

## **2. LEFT JOIN**

A LEFT JOIN (also called LEFT OUTER JOIN) returns all records from the left table and only the matching records from the right table.

If there is no match, the result will contain NULL values for columns from the right table.

<p align="center">
    <img width="310" height="220" alt="image" src="https://github.com/user-attachments/assets/d128279a-083d-49ad-9ccb-2a2d6b01ca90" />

</p>

**Q1. List all employees with their department name (include employees without department).**

```
SELECT e.emp_name, d.dept_name
FROM employees3 e
LEFT JOIN departments3 d
ON e.dept_id = d.dept_id;
```

**Q2. Find employees who do NOT belong to any department.**

```
SELECT e.emp_name
FROM employees3 e
LEFT JOIN departments3 d
ON e.dept_id = d.dept_id
WHERE d.dept_id IS NULL;
```  

**Q3. Show employee name, salary, and department location (even if NULL).**

```
SELECT e.emp_name, e.salary, d.location
FROM employees3 e
LEFT JOIN departments3 d
ON e.dept_id = d.dept_id;
```

**Q4. List all employees and departments; show NULL where department missing.**

```
SELECT e.emp_id, e.emp_name, d.dept_name
FROM employees3 e
LEFT JOIN departments3 d
ON e.dept_id = d.dept_id;
```

**Q5. Count employees per department including unassigned employees.**

```
SELECT d.dept_name, COUNT(e.emp_id) AS total_employees
FROM employees3 e
LEFT JOIN departments3 d
ON e.dept_id = d.dept_id
GROUP BY d.dept_name;
```

## 3. RIGHT JOIN

A RIGHT JOIN (also called RIGHT OUTER JOIN) returns all records from the right table and only the matching records from the left table.

If there is no match, the result will contain NULL values for columns from the left table.

**Q1. List all departments with their employees.**

```
SELECT d.dept_name, e.emp_name
FROM employees3 e
RIGHT JOIN departments3 d
ON e.dept_id = d.dept_id;
```

**Q2. Find departments with no employees.**

```
SELECT d.dept_name
FROM employees3 e
RIGHT JOIN departments3 d
ON e.dept_id = d.dept_id
WHERE e.emp_id IS NULL;
```

**Q3. Show department name and employee salary.**

```
SELECT d.dept_name, e.salary
FROM employees3 e
RIGHT JOIN departments3 d
ON e.dept_id = d.dept_id;
```

**Q4. Count employees in each department (including zero).**

```
SELECT d.dept_name, COUNT(e.emp_id) AS total_employees
FROM employees3 e
RIGHT JOIN departments3 d
ON e.dept_id = d.dept_id
GROUP BY d.dept_name;
```

**Q5. List all department locations with employee names.**

```
SELECT d.location, e.emp_name
FROM employees3 e
RIGHT JOIN departments3 d
ON e.dept_id = d.dept_id;
```

## 4. FULL JOIN

**Q1. List all employees and all departments.**

```
SELECT e.emp_name, d.dept_name
FROM employees3 e
FULL OUTER JOIN departments3 d
ON e.dept_id = d.dept_id;
```

**Q2. Find employees without department and departments without employees.**

```
SELECT e.emp_name, d.dept_name
FROM employees3 e
FULL OUTER JOIN departments3 d
ON e.dept_id = d.dept_id
WHERE e.emp_id IS NULL OR d.dept_id IS NULL;
```

**Q3. Show employee salary and department location.**

```
SELECT e.emp_name, e.salary, d.location
FROM employees3 e
FULL OUTER JOIN departments3 d
ON e.dept_id = d.dept_id;
```

**Q4. Count total rows returned by FULL JOIN.**

```
SELECT COUNT(*)
FROM employees3 e
FULL OUTER JOIN departments3 d
ON e.dept_id = d.dept_id;
```

**Q5. Display unmatched records clearly.**

```
SELECT
  COALESCE(e.emp_name, 'No Employee') AS emp_name,
  COALESCE(d.dept_name, 'No Department') AS dept_name
FROM employees3 e
FULL OUTER JOIN departments3 d
ON e.dept_id = d.dept_id;
```
---

## 🎯 Student Tasks – Module 15: SQL Joins

### Task 1: Basic Join Queries (Easy)
**Objective**: Practice INNER JOIN and LEFT JOIN.

Using `employees3` and `departments3` tables above:
1. List all employee names with their department name using INNER JOIN.
2. Find employees who work in 'Bengaluru'.
3. Show the department name and average salary using INNER JOIN + GROUP BY.
4. Find all employees assigned to IT or Finance departments.

**Expected SQL**:
```sql
SELECT e.emp_name, d.dept_name
FROM employees3 e
INNER JOIN departments3 d ON e.dept_id = d.dept_id;

SELECT e.emp_name, d.location
FROM employees3 e
INNER JOIN departments3 d ON e.dept_id = d.dept_id
WHERE d.location = 'Bengaluru';
```

---

### Task 2: LEFT, RIGHT & FULL JOIN (Medium)
**Objective**: Understand NULL handling across join types.

1. List ALL employees including those with no department (LEFT JOIN).
2. List ALL departments including those with no employees (RIGHT JOIN).
3. Find employees WITHOUT any department assignment.
4. Find departments WITHOUT any employees.
5. Show the result of a FULL OUTER JOIN using UNION (since MySQL doesn't support FULL OUTER JOIN directly).

**Expected SQL**:
```sql
-- Employees without department
SELECT e.emp_name
FROM employees3 e
LEFT JOIN departments3 d ON e.dept_id = d.dept_id
WHERE d.dept_id IS NULL;

-- FULL OUTER JOIN using UNION
SELECT e.emp_name, d.dept_name
FROM employees3 e
LEFT JOIN departments3 d ON e.dept_id = d.dept_id
UNION
SELECT e.emp_name, d.dept_name
FROM employees3 e
RIGHT JOIN departments3 d ON e.dept_id = d.dept_id;
```

---

### Task 3: Multi-table & Self Join (Challenge)
**Objective**: Practice complex join scenarios.

1. Create a `projects` table with `project_id`, `project_name`, `lead_emp_id`.
2. Insert 5 projects assigning employees as leads.
3. Join `projects` with `employees3` and `departments3` to show: project name, lead employee, lead's department, lead's salary.
4. Use SELF JOIN: For each employee, find all colleagues in the same department.
5. Find departments where the total salary of all employees exceeds 100,000.

**Expected SQL**:
```sql
-- Self Join: colleagues in same department
SELECT e1.emp_name AS employee, e2.emp_name AS colleague
FROM employees3 e1
INNER JOIN employees3 e2
  ON e1.dept_id = e2.dept_id
  AND e1.emp_id <> e2.emp_id
ORDER BY e1.emp_name;
```

---

